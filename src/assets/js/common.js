const methods = {

  // changeFunc(idx){
  //   console.log(idx);
  // }

}

export default{
    install(Vue){
        // Vue.prototype.$openFullscreen = methods.openFullscreen;
        // Vue.prototype.$changeFunc = methods.changeFunc;
    }
}